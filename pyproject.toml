[project]
name = "matriz-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.32.5",
    "websocket-client>=1.8.0",
]

[dependency-groups]
dev = [
    "ruff>=0.11.0",
]
